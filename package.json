{
  "name": "que-herramienta-utilizo",
  "version": "0.1.1",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "extract:data": "node scripts/extract-questionnaire.mjs",
    "validate:data": "node scripts/validate-questionnaire.mjs",
    "check:source": "node scripts/check-source.mjs",
    "calibrate": "node scripts/run-calibration.mjs",
    "build": "node scripts/build-static.mjs",
    "test": "node --test",
    "test:e2e": "playwright test",
    "check": "npm run check:source && npm run validate:data && npm test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.61.1",
    "yaml": "2.9.0"
  }
}
